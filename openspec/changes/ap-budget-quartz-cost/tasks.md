# Tasks: ap-budget-quartz-cost

> 前提: `event-lottery-planner` の残課題（`/events` ナビ導線・アイテム名解決）が完了していること。

## Phase 0: データ確定（実装前に出典で固める）
- [ ] D1: マスターレベル → 最大AP テーブル（1〜現行最大レベル200）を確定し定数化。出典: Gamerch 早見表等。既知: ML170→146, ML180→148前後。
- [ ] D2: 各AP回復アイテムの回復量ルールを確定（聖晶石・黄金の果実＝最大AP全回復、白銀/青銅の果実＝固定量 or 割合 を出典で分類）。
- [ ] D3: 聖晶石単価定数を一箇所に定義（現行 ¥10,000 / 168個 ≒ ¥59.5/個）。価格改定で変わりうるため定数化。

## Phase 1: master-profile（マスターレベル同期）
- [ ] `masterLevel` 用 localStorage キー＋アクセサ（未設定時は最大レベル既定）。
- [ ] `use-cloud-sync.ts` の `KEYS` に `masterLevel` を追加（自動同期）。
- [ ] マスターレベル → 最大AP 導出関数（D1 テーブル使用）＋単体テスト。

## Phase 2: AP予算ロジック
- [ ] `lib/ap-budget.ts`（または `event-plan.ts` 拡張）: 必要AP → りんご各種優先消費 → 残りを聖晶石換算 → 課金額化。消費順を定義。
- [ ] 旧固定換算（金果実40/銀林檎20）の表示を本ロジックに置換。
- [ ] 単体テスト: レベル別最大AP・りんご充当・聖晶石枚数・金額（境界: 所持0、過不足ちょうど、最大レベル既定）。

## Phase 3: UI
- [ ] 計画ページにマスターレベル入力（同期）＋所持りんご各種入力（localStorage 端末ローカル）を追加。
- [ ] 消費AP内訳表示（りんご種類ごとの消費数＋聖晶石枚数＋課金額）。
- [ ] 換算式インフォメーション（ツールチップ/注記）。
- [ ] 一覧/計画で終了済みイベントを選択可能化＋会期外バッジ。
- [ ] `locales/{ja,en}.json` にキー追加。

## Phase 4: 検証
- [ ] `pnpm run type-check` / `pnpm run lint` / 全テスト緑。
- [ ] `pnpm dev` + ブラウザ実物確認（マスターレベル変更→最大AP→内訳/金額の連動、同期、終了済みイベント選択、インフォメーション表示）。ユーザー到達導線も確認（オーファン禁止原則）。
- [ ] `openspec validate ap-budget-quartz-cost --strict` → valid。
