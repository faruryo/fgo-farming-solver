## 1. 期待値データ

- [ ] 1.1 proposal の Provenance に従い、第三者の生ドロップ表をリポジトリへ置かず、ゲーム内枠から導出した係数だけを `data/event-craft-recipes.ts` に載せる（出典リンクはコメントまたはファイル先頭）
- [ ] 1.2 水着2026の銅/銀/金対象素材グループと、期待高 0.40・残り均等（銅銀他 0.15、金他 0.12、QP/種火 0）を同ファイルから導出できるようにする
- [ ] 1.3 レシピごとの `yield` マップ（shortId → 期待個数）を純関数でテストする（銅1皿が他銅3種に 0.15）

## 2. ソルバー

- [ ] 2.1 Stage 1 の craft 変数が全対象素材の期待 yield を `item_*` に足し、`cap_*` を外す
- [ ] 2.2 Stage 2 の `V_base` を期待バスケット（孤立コスト×yield の和）にする。`computeSingleItemBaseValues` のキャッシュキーに期待 yield マップ（ソート済み）を含め、ついで係数だけ違う `recipes` を続けて渡しても古い値が使われないテストを追加する
- [ ] 2.3 主産物不足 0・他同レア不足ありで料理が選ばれるケース、ついで込みで残余コストが下がるケースを `lib/event-craft-advisor.test.ts` に追加する
- [ ] 2.4 `calculateAllocatedDeficitSavings` の除外 need は `fullNeed` から当該以外のレシピ期待獲得を引いて作る（クランプ済み残に yield を足し戻さない）。ついで拘束の回帰に加え、小数 yield の過産（例: 不足1に 0.40×3 皿）で `unitSaved` が過大にならないことを確認する
- [ ] 2.5 既存フィクスチャの `yieldCount: 1` 前提を期待値モデルに合わせて直す

## 3. UI

- [ ] 3.1 純関数でおすすめ皿数から育成素材ごとの期待獲得合計を出す（作成数×yield の和、QP/種火除外）。テストで銅料理 N 皿が期待高 0.40N・他銅 0.15N になることを確認する
- [ ] 3.2 結果画面におすすめ割り振りと並んで「この配分での期待獲得」を `t()` で出す（日英 locale）。コンポーネントテストで合計行または内訳が見えることを確認する
- [ ] 3.3 料理カードに1皿あたりの短い内訳を出し、既存カード表示が期待値前提でも落ちないことを確認する

## 4. 検証

- [ ] 4.1 `openspec validate event-craft-expected-yields --strict` と対象テスト、`pnpm run type-check` を通す（アーカイブ前は `validate --specs` だけでは pending delta を見ない）
