## 1. 引き渡しの判断

- [x] 1.1 プレビューホスト、戻り先オリジン、相対パス、許可リスト、分岐の有効判定を pure 関数にする
- [x] 1.2 通常 state の素通し、分岐の無効化、空の許可リスト、メールアドレス不一致、不正な戻り先のテストを追加する

## 2. 暗号とフロー

- [x] 2.1 `pv1.` の state 暗号と、60 秒の引き渡し JWT を WebCrypto で実装する
- [x] 2.2 本番コールバックのコード交換、許可リスト、フラグメント付き 302 を、fetch を注入して実装する
- [x] 2.3 nonce と jti を照合してからセッション用クレームを返す完了処理を実装する
- [x] 2.4 復号失敗と不正オリジンでは fetch しないこと、トークンを Location に出さないこと、jti の再使用を拒否することをテストする

## 3. 経路と画面

- [x] 3.1 Auth.js の GET を分岐で包み、プレビューの開始・完了エンドポイントと完了ページを追加する
- [x] 3.2 サインイン開始をプレビューホストだけ新しい経路にし、本番と localhost は `signIn('google')` のままにする
- [x] 3.3 画面文言を `locales/ja.json` と `locales/en.json` に追加する
- [x] 3.4 `deployment_guide.md` に引き渡し用の環境変数を追記する

## 4. 確認

- [x] 4.1 追加した回帰テストを条件反転で一度失敗させ、戻してから再実行する
- [x] 4.2 `openspec validate --change preview-google-login` を通す
