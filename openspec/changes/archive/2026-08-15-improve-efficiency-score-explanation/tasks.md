## 1. 文言の見直し・共通化

- [x] 1.1 `locales/ja.json` / `locales/en.json` の `効率ポイントとは` を平易な言い回しに書き直す(算出ロジックは変えず、表現のみ見直す)
- [x] 1.2 内訳の列見出し用 i18n キー(素材/重み/寄与度に相当するもの)を `ja.json` / `en.json` 両方に追加する

## 2. 一覧ページの説明導線

- [x] 2.1 `QuestEfficiencyList.tsx` のメイン行にある既存の `Info` アイコン + `Tooltip` を、より視認しやすい配置・見た目に調整する(位置・ラベル付与などスコア表示の近くから辿れることを優先)
- [x] 2.2 フィルターポップオーバー末尾の説明キャプションが 1.1 で更新した文言を参照していることを確認する

## 3. 詳細ページの説明・内訳ラベル

- [x] 3.1 `QuestEfficiencyCard.tsx` の「効率ポイント」見出し横に、一覧と同じ `Tooltip` パターンで説明アイコンを追加する(1.1 の共通キーを参照)
- [x] 3.2 内訳リスト(`eff.contributions` の表示部分)の直前に、素材名・重み・寄与度に対応する列見出し行を追加する(1.2 の新規キーを使用)

## 4. 検証

- [x] 4.1 `pnpm dev` で `/quests` を開き、一覧の説明アイコンからツールチップが表示されることを確認する(chrome-devtoolsで実機確認済み。ラベル付きピルが表示され、ホバーで新文言のツールチップが出ることを確認)
- [x] 4.2 `/quests/[id]` を開き、詳細ページの説明ツールチップと内訳の列見出しが表示されることを確認する(実機確認済み。見出し横のInfoアイコンから同じ説明文言、内訳リストに「素材/重み/寄与度」の列見出しが表示されることを確認)
- [x] 4.3 日本語UI・英語UIの両方で表示崩れ・キー欠落(生キー表示)がないことを確認する(日本語UIは実機確認済み。英語UIはこのアプリに現状ランタイムの言語切替導線が無い(`lib/i18n.ts` で `lng: 'ja'` 固定、`changeLanguage` 呼び出し箇所なし)ため実画面での確認は不可。`locales/en.json` へのキー追加はJSONとして妥当で、追加キーが`ja.json`と1対1で揃っていることを確認済み)
- [x] 4.4 型チェック・既存テストを実行し、表示のみの変更で計算結果に影響がないことを確認する
