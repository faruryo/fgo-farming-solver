## 1. 準備とコンポーネントの共通化

- [x] 1.1 `components/farming/FarmingHistoryChart.tsx` を新規作成し、共通のグラフ基盤を実装する
- [x] 1.2 `HistoryGraph.tsx` から `FarmingHistoryChart` を使用するようにリファクタリングする
- [x] 1.3 `app/farming/history/page.tsx` から `FarmingHistoryChart` を使用するようにリファクタリングする

## 2. タイムライン表示と期間フィルタリングの実装

- [x] 2.1 Recharts の XAxis を `type="number"` に変更し、UNIX タイムスタンプによるプロットを実装する
- [x] 2.2 `PeriodRange` ('1m' | '3m' | '6m' | '1y' | 'all') の状態管理とセレクターUIを実装する
- [x] 2.3 選択された期間に基づいてデータポイントをフィルタリングするロジックを実装する

## 3. 達成予測ロジックの実装

- [x] 3.1 最小二乗法による線形回帰（傾き・切片の算出）ヘルパー関数を実装する
- [x] 3.2 フィルタリングされたデータから達成予想日（Y=0となるX）を計算するロジックを追加する
- [x] 3.3 Recharts の `Line` を使用して、実績データの終点から予想日までの予測線を描画する

## 4. UI/UX の調整と仕上げ

- [x] 4.1 達成予想日をツールチップまたはグラフ上のラベルとして表示する
- [x] 4.2 期間切り替え時のアニメーションやスケルトン表示を調整する
- [x] 4.3 データ不足時や予測不能時のフォールバック表示（メッセージ）を実装する
