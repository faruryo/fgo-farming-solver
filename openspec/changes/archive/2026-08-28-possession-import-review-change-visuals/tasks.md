## 1. 分類・並びの純関数

- [x] 1.1 `lib/possession-import/review-presentation.ts` に `classifyQuantityChange` / `signedDelta` / `reviewSection` / `compareReviewRows` と、フィルタ適用・セクション分割のヘルパーを追加する（design.md D1）。
- [x] 1.2 `lib/possession-import/review-presentation.test.ts` にケース表を追加する: 増加・減少・変更なし・`proposed=null`（unknown）、要確認が増減より先、同一クラス内は `|delta|` 降順→名前昇順、フィルタ「変更あり」「要確認」。
- [x] 1.3 1.2 の新規テストが、実装前または条件反転で実際に赤くなることを確認してから実装を通す。

## 2. 統合結果の並びを分離する

- [x] 2.1 `mergeCandidates` から `needsReview` 先頭ソートを外し、Atlas ID 昇順など安定した順序だけを返す。
- [x] 2.2 `merge-candidates.test.ts` に、統合結果の順序がレビュー表示順に依存しないこと（または Atlas ID 順であること）を残す。表示順の契約は 1.2 側に置く。

## 3. i18n

- [x] 3.1 `locales/ja.json` / `locales/en.json` の `quests` に、サマリ・フィルタ（すべて / 変更あり / 要確認）・セクション見出し・符号バッジ用 aria の kebab-case キーを両方へ追加する。

## 4. レビューUI

- [x] 4.1 `PossessionImportDialog` の review 段階で、編集中の入力値を `parsePossessionInput` して 1.1 の純関数に渡し、サマリ・フィルタ・セクション見出しを描画する。
- [x] 4.2 増加は青緑薄塗り + 実線左縁 + `+N`、減少はオレンジ薄塗り + 破線左縁 + `-N`、変更なしはコントラスト低下・符号なし。減少色に `--destructive` を使わない（D2）。
- [x] 4.3 変更なしセクションは件数が 1 以上なら既定で折りたたみ、見出しの件数から展開できる。フィルタ「すべて」以外では変更なし見出しを出さない。
- [x] 4.4 新規文言は `t('kebab-key', '日本語フォールバック')`。既存キー（矛盾あり / 要確認 / 反映する 等）は改名しない。
- [x] 4.5 クロップ展開は要確認行のみのままにする。フィルタで隠れた行・折りたたんだ変更なし行も、除外していなければ確定対象のままにする。

## 5. Dialog テスト

- [x] 5.1 `PossessionImportDialog.test.tsx` の `t` mock を `t: (key, fallback) => fallback ?? key` に寄せ、除外テストはチェック位置ではなくアイテム名で行を取る。
- [x] 5.2 増加 / 減少 / 変更なしのバッジまたは識別属性が出ること、変更なしが初期表示で折りたたまれ件数が見えること、フィルタ「変更あり」で変更なしが消えること、値の編集で分類が移ることを検証する。
- [x] 5.3 折りたたみ中・「変更あり」フィルタ中でも、除外していない変更なし行が `onConfirm` に含まれることを検証する。
- [x] 5.4 既存の確定・修正・除外・キャンセル・矛盾・ゼロ件・クロップのテストが通ることを確認する。

## 6. 検証

- [x] 6.1 `pnpm test` と `pnpm run type-check` を通し、対象テストが意図どおり赤→緑になった記録を残す。
- [x] 6.2 ブラウザでレビュー画面を開き、(a) 3種の見分け、(b) 既定並び、(c) 変更なし折りたたみ、(d) フィルタ、(e) 値編集後のサマリ／セクション移動、(f) 折りたたみまま反映、を確認する。dev server は起動済みであること（未起動なら起動を依頼してから検証する）。
