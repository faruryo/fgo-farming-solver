## 1. 共通ロジックと移行

- [x] 1.1 `.agents/rules/testing.instructions.md` を読み、周回目的型、低在庫倍率、対象判定、有限目標、旧設定移行の失敗テストを先に追加する
- [x] 1.2 `lib/quest-efficiency.ts` の既存バッファー判定を再利用し、3モードの共有純関数を最小構成で実装する
- [x] 1.3 新しい保存キーを既存のlocalStorage・タブ間同期・クラウド同期へ追加し、新値を優先する一度限りの移行を実装する

## 2. 共通UI

- [x] 2.1 既存のハンバーガー `Sheet` と影響画面で再利用する3択UIを追加し、選択変更を即時反映する
- [x] 2.2 既存の `Dialog` で計算方法を表示し、3モード、低在庫倍率、AP効率・周回効率、報酬加算を説明する
- [x] 2.3 モード名、説明、在庫基準の文言を `t()` 経由で追加し、日英localeとテストmockを更新する
- [x] 2.4 モード選択とDialogのキーボード操作、フォーカス復帰、タブ間同期を検証する

## 3. クエスト効率

- [x] 3.1 クエスト一覧と詳細を共有モード・低在庫倍率へ切り替え、旧「不足のみ／全部」と「ストック込み」操作を削除する
- [x] 3.2 一覧・詳細に現在モードと変更導線を表示し、0点時も操作可能にする
- [x] 3.3 指定例、レアリティ別バッファー、未入力所持数、0バッファー、各分母の回帰テストを追加する

## 4. 数量を使う画面

- [x] 4.1 育成結果からソルバーへ送る目標を選択モードの単一目標へ変更し、2目標同時計算と `required + buffer` を削除する
- [x] 4.2 配布アドバイザーと進捗表示を共有の有限目標へ切り替え、`all` では `training` を使う注記を表示する
- [x] 4.3 手入力ソルバーと過去履歴を変えず、`training`・`reserve`・`all` の回帰テストを追加する

## 5. 検証

- [x] 5.1 対象テスト、`pnpm run type-check`、`pnpm run lint:ratchet` を実行して回帰がないことを確認する
- [x] 5.2 `openspec validate farming-purpose-modes --strict` を実行し、仕様と実装の整合を確認する
